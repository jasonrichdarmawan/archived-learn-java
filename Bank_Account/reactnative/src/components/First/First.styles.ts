import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  flexcenter: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  wrapperPressable: {
    borderRadius: 8,
    marginVertical: 10,
    marginHorizontal: 60,
    padding: 60,
    alignItems: 'center',
    alignSelf: 'stretch',
  },
});
